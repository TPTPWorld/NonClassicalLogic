# underspecification: missing identifier for constants
thf(1,logic,(
    $modal ==
        [ $consequence == $local,
          $quantification == $constant,
          $modalities == $modal_system_S5] )).

thf(2, type, a: $i).
